_parent_: options/barf_llff.yaml

data:                                                       # data options
    root: 
    dataset: colmap                                   # dataset name
    scene: "shoes"                                     # scene name
    image_size: [1080,1920]                                 # input image sizes [height,width]
    # subsample: 5

nerf:
  rand_rays: 2048                                           # number of random rays for each step

freq:
  vis: 10000
  val: 10000